<div class="container">
    <div class="d-flex justify-content-between">
        <a href="/profile/{{user.username}}" class="btn btn-primary mt-5" style="height: fit-content;"><i
                class="fas fa-arrow-left"></i> Back</a>
        <h1 class="text-center p-5">Delete your account</h1>
        <a href="#"></a>
    </div>
    <div class="d-flex justify-content-center ">
        <a class="btn btn-primary" id="delete_account" href="/delete/redirect/{{auth.username}}">Click here</a>
    </div>
</div>